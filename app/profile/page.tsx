// Page to view my campaigns & contributions as a user with a connected wallet.
// Would be linked from the top right dropdown menu.

import { Flex } from "@chakra-ui/react";

const ProfilePage = () => {
  return <Flex>Profile</Flex>;
};
export default ProfilePage;
